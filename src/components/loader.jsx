import React from 'react';

/**
 * Function that displays "fetching data"
 *
 * @return {React.ReactElement} A React component.
 */

export const Loader = () => {
  return <div>Fetching data</div>;
};
